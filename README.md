# prithi

This project is a personal portfolio built with React and Vite. It includes a hero section with a background video, responsive layout, and several components showcasing projects and skills.

Development notes:
- Run `npm install` to install dependencies.
- Start the dev server with `npm run dev`.
- Build for production with `npm run build`.

This README replaces merge conflict markers and provides a brief project overview.
